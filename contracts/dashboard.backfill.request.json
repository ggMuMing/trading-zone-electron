{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trading-zone.local/contracts/dashboard.backfill.request.json",
  "title": "DashboardBackfillRequest",
  "description": "Params for method data.sync.dashboard_backfill",
  "type": "object",
  "required": ["token", "start_date", "end_date"],
  "properties": {
    "token": { "type": "string", "minLength": 1 },
    "start_date": { "type": "string", "pattern": "^[0-9]{8}$" },
    "end_date": { "type": "string", "pattern": "^[0-9]{8}$" }
  },
  "additionalProperties": false
}
